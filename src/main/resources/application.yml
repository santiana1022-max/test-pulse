server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    # ? ??? 1?????????????????? JDK17+MySQL8 ???????????? Bug
    url: jdbc:mysql://localhost:3306/test_pulse?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: Sanyuan1022

  jackson:
    # ? ??? 2???????????????? DTO ????? @JsonFormat ??
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8

mybatis-plus:
  configuration:
    # ????? SQL???????
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    # ???????? (???? true??????????)
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      # ? ??? 3????????????????? Entity ????? @TableId(type = IdType.AUTO) ?
      id-type: auto
      # ? ??? 4????????????????????????
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0