server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/test_pulse?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password: Sanyuan1022

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl # ?????? SQL ???????
  global-config:
    db-config:
      logic-delete-field: deleted # ???????